name: benchmark_env
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.10
  - numpy
  - cython         # <-- moved from pip to conda
  - setuptools     # <-- moved from pip to conda
  - wheel          # <-- moved from pip to conda
  - pip
  - pip:
      - numba
      - pandas
      - matplotlib
      - tqdm
      - scipy
      - statsmodels
      - scikit-learn
      - xgboost
      - pyranges
      - anndata
      - mudata
      - muon
      - typing_extensions
      - celloracle
      - decoupler
